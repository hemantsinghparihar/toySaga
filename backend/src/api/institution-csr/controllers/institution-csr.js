'use strict';

/**
 * institution-csr controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::institution-csr.institution-csr');
